#include <stdio.h>

/**
 * main - print the string "with proper grammar, but the
 * outcome is a piece of art,
 * Return:  will return 0 if properly done
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
